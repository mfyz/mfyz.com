# Get the latest commit hash
COMMIT_HASH=$(git rev-parse HEAD)

# The GitHub repository URL (adjust if needed)
REPO_URL="https://github.com/mfyz/mfyz.com"

# Open the commit in the default browser
open "$REPO_URL/commit/$COMMIT_HASH"
