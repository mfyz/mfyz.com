# Run lint-staged to process only staged files
npm run lint-staged

# Ensure there are no TypeScript errors
echo "Checking TypeScript..."
npm run check

# Run a subset of tests that are fast
echo "Running critical tests..."
npm run test
