---
import HeaderLink from "./HeaderLink.astro";
import { SITE } from "../consts";
import DarkModeToggle from "./DarkModeToggle";
---

<header class="fixed left-0 top-0 z-50 w-full backdrop-blur-sm bg-white/80 dark:bg-gray-900/80 supports-[backdrop-filter]:bg-white/60 dark:supports-[backdrop-filter]:bg-gray-900/60">
  <nav class="container">
    <h2><a href="/" class="font-secondary text-xl dark:text-white">{SITE.title}</a></h2>
    <div class="flex items-center gap-4">
      <HeaderLink href="/about">About</HeaderLink>
      <HeaderLink href="/tr">🇹🇷</HeaderLink>
      <DarkModeToggle client:load />
    </div>
  </nav>
</header>

<style>
  h2 a,
  h2 a.active {
    text-decoration: none;
  }
  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 0;
  }

  nav a {
    padding: 0.5em 0.5em;
    text-decoration: none;
  }
</style>
